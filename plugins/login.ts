import { createApp } from 'vue'

export default defineNuxtPlugin((nuxtApp) => {
  // nuxtApp.vueApp.directive('login', {
  //   beforeUpdate(el: HTMLElement) {
  //     if ( userStore)
  //   },
  // })
})
